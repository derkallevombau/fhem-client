{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "npm",
			"script": "compile",
			"group": {
                "kind": "build",
				"isDefault": true
			},
			"problemMatcher": [
                "$tsc"
			],
			"label": "npm: compile",
			"detail": "npm run generateJs && npm run generateDts && npm run generateDoc"
		}
	]
}
